{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python check_env.py && python manage.py migrate --noinput && python test_urls.py && echo 'Starting Django development server...' && python manage.py runserver 0.0.0.0:$PORT --noreload",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
